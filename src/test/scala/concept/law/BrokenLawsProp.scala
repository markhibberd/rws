package concept.law

import org.scalacheck._, Prop._

object BrokenLawsProp extends Properties("BrokenLaws") {
//  property("Double addition is associative! ") = forAll(
//    (x: Double, y: Double, z: Double) => ((x + y) + z) == (x + (y + z)))
}
